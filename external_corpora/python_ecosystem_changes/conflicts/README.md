# Conflict Snippets

Put optional conflicting or narrowing guidance snippets here.

Use the filenames listed in:

- `../manifest.template.json`

These files are useful when two active sources should remain visible together instead of being silently merged.
